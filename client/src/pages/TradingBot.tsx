import { useState } from "react";
import { ArrowLeft, CheckCircle2, Eye, EyeOff, LockKeyhole, Power, ShieldAlert, WalletCards } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Switch } from "@/components/ui/switch";
import { useTheme } from "@/contexts/ThemeContext";

type SecretInputProps = { label: string; value: string; onChange: (value: string) => void; placeholder: string };
function SecretInput({ label, value, onChange, placeholder }: SecretInputProps) {
  const [visible, setVisible] = useState(false);
  return <div><Label className="text-xs text-muted-foreground">{label}</Label><div className="relative mt-1"><Input type={visible ? "text" : "password"} value={value} onChange={event => onChange(event.target.value)} placeholder={placeholder} autoComplete="off" className="pr-10" /><button type="button" onClick={() => setVisible(current => !current)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded p-1 text-muted-foreground hover:text-foreground" aria-label={visible ? `Ẩn ${label}` : `Hiện ${label}`}>{visible ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div></div>;
}

export default function TradingBot() {
  const { theme, toggleTheme } = useTheme();
  const [binanceKey, setBinanceKey] = useState("");
  const [binanceSecret, setBinanceSecret] = useState("");
  const [okxKey, setOkxKey] = useState("");
  const [okxSecret, setOkxSecret] = useState("");
  const [dexPassphrase, setDexPassphrase] = useState("");
  const [offlineMode, setOfflineMode] = useState(true);
  const [wallet, setWallet] = useState("");
  const [chain, setChain] = useState("BSC");
  const [armed, setArmed] = useState(false);
  const [confirmation, setConfirmation] = useState(true);
  const [maxRisk, setMaxRisk] = useState("1");
  const [message, setMessage] = useState("");
  const saveDraft = () => setMessage("Đã kiểm tra biểu mẫu cục bộ. Credentials thật phải được lưu qua kho secrets server-side; chưa có lệnh nào được gửi.");
  return <main className="min-h-screen bg-background px-4 py-6 text-foreground md:px-8"><div className="mx-auto max-w-6xl"><header className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-primary">Execution Lab</p><h1 className="mt-2 text-3xl font-semibold">Trading Bot</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Kết nối phân tích BTC/ETH với Binance, OKX và ví DEX. Bot chỉ hoạt động khi người dùng chủ động bật và xác nhận lệnh.</p></div><div className="flex items-center gap-2"><Button variant="outline" size="icon" onClick={toggleTheme} aria-label="Đổi giao diện">{theme === "dark" ? "☼" : "◐"}</Button><Button variant="outline" onClick={() => { window.location.href = "/"; }}><ArrowLeft className="mr-2 h-4 w-4" />Dashboard</Button></div></header><div className="mb-5 grid gap-3 md:grid-cols-3"><Card><CardContent className="flex items-center gap-3 p-4"><LockKeyhole className="h-5 w-5 text-primary" /><div><p className="text-xs font-semibold">Secrets server-side</p><p className="mt-1 text-xs text-muted-foreground">Không hiển thị lại API secret.</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-4"><ShieldAlert className="h-5 w-5 text-amber-500" /><div><p className="text-xs font-semibold">Không quyền rút tiền</p><p className="mt-1 text-xs text-muted-foreground">Chỉ Read/Trade tối thiểu.</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-4"><CheckCircle2 className={confirmation ? "h-5 w-5 text-emerald-500" : "h-5 w-5 text-muted-foreground"} /><div><p className="text-xs font-semibold">Xác nhận từng lệnh</p><p className="mt-1 text-xs text-muted-foreground">Bắt buộc trước live execution.</p></div></CardContent></Card></div><div className="mb-5 flex items-center justify-between rounded-xl border border-primary/20 bg-primary/5 p-4"><div><p className="text-sm font-semibold">Chế độ kiểm thử offline</p><p className="mt-1 text-xs text-muted-foreground">Bỏ qua API sàn, chỉ kiểm tra giao diện và luồng phân tích; không gửi lệnh thật.</p></div><Switch checked={offlineMode} onCheckedChange={setOfflineMode} /></div><div className="grid gap-5 lg:grid-cols-2">{offlineMode ? <Card className="lg:col-span-2"><CardContent className="flex items-start gap-3 p-5"><ShieldAlert className="mt-0.5 h-5 w-5 text-primary" /><div><p className="text-sm font-semibold">Binance/OKX đang được bỏ qua</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Tắt Chế độ kiểm thử offline nếu muốn hiển thị lại form API. Hiện tại hệ thống không đọc credentials, không kiểm tra kết nối và không đặt lệnh.</p></div></CardContent></Card> : <><Card><CardHeader><CardTitle className="text-base">Binance · CEX</CardTitle></CardHeader><CardContent className="space-y-4"><SecretInput label="API Key" value={binanceKey} onChange={setBinanceKey} placeholder="Nhập API key Read/Trade" /><SecretInput label="API Secret" value={binanceSecret} onChange={setBinanceSecret} placeholder="Nhập API secret" /><p className="rounded-lg border border-amber-500/20 bg-amber-500/5 p-3 text-xs leading-5 text-muted-foreground">Tắt Withdraw và bật IP whitelist trên Binance trước khi kết nối.</p></CardContent></Card><Card><CardHeader><CardTitle className="text-base">OKX · CEX</CardTitle></CardHeader><CardContent className="space-y-4"><SecretInput label="API Key" value={okxKey} onChange={setOkxKey} placeholder="Nhập API key Read/Trade" /><SecretInput label="Secret Key" value={okxSecret} onChange={setOkxSecret} placeholder="Nhập secret key" /><p className="rounded-lg border border-amber-500/20 bg-amber-500/5 p-3 text-xs leading-5 text-muted-foreground">Chỉ bật Read/Trade, tắt Withdraw và liên kết IP nếu có thể.</p></CardContent></Card></>}<Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><WalletCards className="h-4 w-4 text-primary" />Ví DEX · EVM/BSC</CardTitle></CardHeader><CardContent className="space-y-4"><div><Label className="text-xs text-muted-foreground">Blockchain</Label><select value={chain} onChange={event => setChain(event.target.value)} className="mt-1 h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>EVM</option><option>BSC</option></select></div><div><Label className="text-xs text-muted-foreground">Địa chỉ ví công khai</Label><Input value={wallet} onChange={event => setWallet(event.target.value)} placeholder="0x..." className="mt-1" /></div><SecretInput label="Passphrase ví DEX" value={dexPassphrase} onChange={setDexPassphrase} placeholder="Chỉ dùng cho ghi chú phiên ví; không phải private key" /><p className="rounded-lg border border-primary/20 bg-primary/5 p-3 text-xs leading-5 text-muted-foreground">Không nhập private key. Khi giao dịch DEX, ví sẽ yêu cầu người dùng tự ký từng giao dịch.</p></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Power className="h-4 w-4 text-primary" />Giới hạn vận hành</CardTitle></CardHeader><CardContent className="space-y-4"><div className="flex items-center justify-between rounded-lg border border-border p-3"><div><p className="text-sm font-medium">Cho phép live bot</p><p className="text-xs text-muted-foreground">Mặc định tắt để tránh gửi lệnh ngoài ý muốn.</p></div><Switch checked={armed} onCheckedChange={setArmed} /></div><div className="flex items-center justify-between rounded-lg border border-border p-3"><div><p className="text-sm font-medium">Xác nhận từng lệnh</p><p className="text-xs text-muted-foreground">Không cho phép tự động bỏ qua confirmation.</p></div><Switch checked={confirmation} onCheckedChange={setConfirmation} /></div><div><Label className="text-xs text-muted-foreground">Rủi ro tối đa mỗi lệnh (%)</Label><Input value={maxRisk} onChange={event => setMaxRisk(event.target.value)} type="number" min="0.1" max="5" step="0.1" className="mt-1" /></div><Button onClick={saveDraft} className="w-full">Kiểm tra và lưu cấu hình</Button>{message && <p role="status" className="text-xs leading-5 text-emerald-600 dark:text-emerald-300">{message}</p>}</CardContent></Card></div><Card className="mt-5 border-rose-500/20 bg-rose-500/5"><CardContent className="p-4 text-xs leading-5 text-muted-foreground"><strong className="text-foreground">Trạng thái an toàn:</strong> live bot hiện {armed ? "được bật ở giao diện, nhưng vẫn yêu cầu xác nhận từng lệnh" : "đang tắt"}. Tín hiệu từ engine chỉ là nguồn phân tích; {offlineMode ? "chế độ offline đang bật và không có lệnh nào được đặt." : "không có lệnh nào được đặt tự động trong phiên này."}</CardContent></Card></div></main>;
}
