import { useMemo, useState } from "react";
import { ArrowLeft, ChartNoAxesCombined, Loader2, ShieldCheck } from "lucide-react";
import { CartesianGrid, Line, LineChart, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { formatConfidencePoint, summarizeConfidenceTimeline } from "@/lib/confidenceTimeline";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const exchanges = ["Binance", "Bybit", "OKX"] as const;
const symbols = ["BTCUSDT", "ETHUSDT"] as const;
const intervals = ["15m", "1h", "4h", "1d"] as const;
const tooltipStyle = { background: "var(--popover)", color: "var(--popover-foreground)", border: "1px solid var(--border)", borderRadius: 10, fontSize: 12 };

function SelectControl({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: readonly string[] }) {
  return <label className="text-xs text-muted-foreground">{label}<select className="mt-1 h-10 w-full rounded-md border border-input bg-background px-3 text-sm text-foreground" value={value} onChange={event => onChange(event.target.value)}>{options.map(option => <option key={option} value={option}>{option.replace("USDT", "")}</option>)}</select></label>;
}

function Metric({ label, value, note }: { label: string; value: string; note: string }) { return <Card><CardContent className="p-4"><p className="text-[10px] font-semibold uppercase tracking-[.14em] text-muted-foreground">{label}</p><p className="mt-2 text-xl font-semibold">{value}</p><p className="mt-1 text-xs text-muted-foreground">{note}</p></CardContent></Card>; }

export default function ConfidenceTimeline() {
  const { user, loading } = useAuth();
  const [exchange, setExchange] = useState<(typeof exchanges)[number]>("Binance");
  const [symbol, setSymbol] = useState<(typeof symbols)[number]>("BTCUSDT");
  const [interval, setInterval] = useState<(typeof intervals)[number]>("1h");
  const [limit, setLimit] = useState(36);
  const history = trpc.market.confidenceHistory.useQuery({ exchange, symbol, interval, limit }, { enabled: Boolean(user), refetchInterval: 30_000 });
  const points = history.data ?? [];
  const summary = useMemo(() => summarizeConfidenceTimeline(points), [points]);
  const chartData = useMemo(() => points.map(point => ({ ...point, ...formatConfidencePoint(point) })), [points]);
  if (loading) return <div className="grid min-h-screen place-items-center bg-background"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div>;
  if (!user) return <div className="grid min-h-screen place-items-center bg-background p-6 text-center"><div><p className="text-sm text-muted-foreground">Đăng nhập để xem timeline confidence của tài khoản.</p><Button className="mt-4" onClick={() => startLogin()}>Đăng nhập</Button></div></div>;
  return <main className="min-h-screen bg-background px-4 py-7 text-foreground lg:px-8"><div className="mx-auto max-w-[1240px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><Button variant="ghost" size="sm" onClick={() => { window.location.href = "/?page=platform"; }}><ArrowLeft className="mr-1 h-4 w-4" /> Platform Overview</Button><p className="mt-5 text-xs font-semibold uppercase tracking-[.2em] text-primary">Signal observability</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Confidence Timeline</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">Theo dõi confidence sau quality gate của snapshot đã lưu, tách rõ theo sàn, BTC/ETH và từng khung. Dữ liệu lịch sử thiếu confidence không được nội suy.</p></div><Button variant="outline" onClick={() => { window.location.href = "/?page=command-center"; }}><ChartNoAxesCombined className="mr-2 h-4 w-4" /> Command Center</Button></div><Card className="mt-6"><CardContent className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-4"><SelectControl label="Sàn" value={exchange} onChange={value => setExchange(value as typeof exchange)} options={exchanges} /><SelectControl label="Tài sản" value={symbol} onChange={value => setSymbol(value as typeof symbol)} options={symbols} /><SelectControl label="Khung thời gian" value={interval} onChange={value => setInterval(value as typeof interval)} options={intervals} /><SelectControl label="Số snapshot" value={String(limit)} onChange={value => setLimit(Number(value))} options={["12", "24", "36", "60"]} /></CardContent></Card>{history.isLoading ? <div className="grid min-h-[340px] place-items-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div> : history.error ? <Card className="mt-5 border-destructive/30 bg-destructive/5"><CardContent className="p-5 text-sm text-destructive">Không thể tải confidence history. Hãy thử lại sau.</CardContent></Card> : <><section className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><Metric label="Snapshots" value={String(summary.observations)} note={`${symbol.replace("USDT", "")} · ${interval} · ${exchange}`} /><Metric label="Confidence TB" value={summary.average == null ? "—" : `${summary.average.toFixed(1)}/100`} note="Chỉ từ snapshot có confidence" /><Metric label="Cao nhất" value={summary.max == null ? "—" : `${summary.max.toFixed(0)}/100`} note="Không ngoại suy dữ liệu thiếu" /><Metric label="Thấp nhất" value={summary.min == null ? "—" : `${summary.min.toFixed(0)}/100`} note="Sau khi quality gate áp dụng" /><Metric label="Quality-gated" value={String(summary.gated)} note="Snapshot có isTradeEligible=false" /></section><Card className="mt-5"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><ChartNoAxesCombined className="h-4 w-4 text-primary" /> Diễn biến confidence theo nến đã đóng</CardTitle></CardHeader><CardContent><div className="h-[360px] w-full">{chartData.length >= 2 ? <ResponsiveContainer width="100%" height="100%"><LineChart data={chartData} margin={{ top: 14, right: 18, left: -10, bottom: 32 }}><CartesianGrid strokeDasharray="3 3" stroke="var(--border)" /><XAxis dataKey="time" minTickGap={34} tick={{ fill: "currentColor", fontSize: 10 }} /><YAxis domain={[0, 100]} tick={{ fill: "currentColor", fontSize: 10 }} /><ReferenceLine y={50} stroke="#f59e0b" strokeDasharray="4 4" label={{ value: "50", fill: "#f59e0b", fontSize: 10 }} /><ReferenceLine y={70} stroke="#10b981" strokeDasharray="4 4" label={{ value: "70", fill: "#10b981", fontSize: 10 }} /><Tooltip contentStyle={tooltipStyle} formatter={(value: number, name: string) => [name === "confidence" ? `${value}/100` : value, name === "confidence" ? "Confidence" : name]} labelFormatter={label => `Nến đóng: ${label}`} /><Line type="monotone" dataKey="confidence" name="confidence" stroke="var(--primary)" strokeWidth={2.5} dot={{ r: 3, fill: "var(--primary)", strokeWidth: 0 }} activeDot={{ r: 5 }} isAnimationActive={false} /></LineChart></ResponsiveContainer> : <div className="grid h-full place-items-center rounded-xl border border-dashed border-border px-6 text-center text-sm leading-6 text-muted-foreground">Cần tối thiểu hai snapshot có confidence cho tổ hợp đang chọn. Snapshot cũ không có trường confidence sẽ không được thay bằng giá trị ước lượng.</div>}</div><p className="mt-3 text-xs leading-5 text-muted-foreground">Đường tham chiếu 50 và 70 chỉ là mốc quan sát confidence, không phải tín hiệu giao dịch hay cam kết hiệu suất.</p></CardContent></Card><Card className="mt-5"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><ShieldCheck className="h-4 w-4 text-primary" /> Decision context từng snapshot</CardTitle></CardHeader><CardContent>{chartData.length ? <div className="max-h-[360px] overflow-auto"><table className="w-full min-w-[680px] text-left text-xs"><thead className="sticky top-0 bg-card text-[10px] uppercase tracking-[.14em] text-muted-foreground"><tr><th className="px-2 py-3">Nến đóng</th><th className="px-2 py-3">Confidence</th><th className="px-2 py-3">Xu hướng</th><th className="px-2 py-3">Quality penalty</th><th className="px-2 py-3">Quality gate</th></tr></thead><tbody>{chartData.map(point => <tr key={`${point.candleClosedAt}-${point.confidence}`} className="border-t border-border"><td className="px-2 py-3 text-muted-foreground">{point.time}</td><td className="px-2 py-3 font-semibold text-primary">{point.confidence}/100</td><td className="px-2 py-3">{point.trend}</td><td className="px-2 py-3">{point.penalty}</td><td className={point.quality === "Quality-gated" ? "px-2 py-3 text-amber-600 dark:text-amber-300" : "px-2 py-3 text-muted-foreground"}>{point.quality}</td></tr>)}</tbody></table></div> : <p className="rounded-xl border border-dashed border-border p-5 text-sm text-muted-foreground">Chưa có confidence snapshot phù hợp với tổ hợp đang chọn.</p>}</CardContent></Card></>}</div></main>;
}
